<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.haemin.spring.test.thymeleaf.repository.WeatherRepository">
 	<select id="selectWeatherHistory"
 	resultType="com.haemin.spring.test.thymeleaf.domain.Weather">
 		SELECT
 			`id`
 			, `date`
 			, `weather`
 			, `temperatures`
 			, <!-- 하다 말음 -->
 	
 	</select>
 </mapper>